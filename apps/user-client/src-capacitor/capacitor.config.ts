import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.wechat.ai.operations',
  appName: '微信公众号 AI 运营助手',
  webDir: 'www',
  server: { androidScheme: 'https' },
}

export default config
